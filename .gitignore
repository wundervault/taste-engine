# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local state
*.log
/tmp/
.streamlit/secrets.toml

# Large raw data — regenerable from BD pulls
data/maps_raw/
data/maps_reviews_raw/
data/yelp_search_html_cache/

# Source pantry photos (user-emailed BYO screenshots) — transcribed already into JSON
# loaders. Originals not needed at runtime.
data/sweetgreen_byo/
data/cava_byo/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Hackathon submission artifacts — not project files. Lives outside the repo.
SUBMISSION.md
SUBMISSION.html
VIDEO_SCRIPT.md
VIDEO_SCRIPT.html
DESIGN_AUDIT.md
DESIGN_AUDIT.html
HANDOFF.md
HANDOFF-*.md

# Entire build output dir (PDFs, slides, screenshots) — generated, not source
dist/

# Retired LLM-generated prose surface (subagent output that bypassed our audits;
# now replaced by deterministic content from quarterly_brief.json + signal
# ranking + pantry_fit). Kept locally for reference, not in the repo.
data/exec_overviews.json
data/overview_snapshot.json

# Old planning + debugging notes — not project source
hackathon-scope.md
vault_exec_problems.md
test-write
